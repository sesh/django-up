description "Runner for {{ app_name }}"
start on runlevel [2345]
stop on runlevel [06]
respawn
respawn limit 10 5

setuid {{ app_name }}
setgid {{ app_name }}
exec /srv/www/{{ app_name }}/{{ app_name }}.sh
